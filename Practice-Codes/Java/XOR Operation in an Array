class Solution 
{
    public int xorOperation(int n, int start) 
    {
        int current = start + 2 * 0;
        for(int i = 1; i < n; i++)
        {
            int xor = start + 2 * i;
            current = current ^ xor;
        } 
        return current;
    }
}
